import { StyleSheet } from "react-native";

homeScreenStyles = StyleSheet.create({
  imageBackground: {
    width: '100%',
    height: 200,
    resizeMode: 'cover',
  },
  overlayText: {
    position: 'absolute',
    alignItems: 'left',
    marginTop: 50
  },
  overlayButtons: {
    position: 'absolute',
    alignItems: 'center',
    marginTop: 120
  },

})

export default homeScreenStyles
